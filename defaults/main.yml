daily_commands_apply_updates: yes
daily_commands_download_updates: yes
daily_commands_random_sleep: "{{ 360 |random(seed=inventory_hostname) }}"
daily_commands_update_cmd: security
daily_commands_update_messages: yes
daily_email_email_from: "update@{{ inventory_hostname }}"
daily_emitters_system_name: "{{ inventory_hostname }}"
hourly_commands_apply_updates: no
hourly_commands_download_updates: yes
hourly_commands_random_sleep: "{{ 59 |random(seed=inventory_hostname) }}"
hourly_commands_update_cmd: security
hourly_commands_update_messages: yes
hourly_email_email_from: "update@{{ inventory_hostname }}"
hourly_emitters_system_name: "{{ inventory_hostname }}"
